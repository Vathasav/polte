[api]
api-node ansible_ssh_host={{ hostvars['api-node']['ansible_ssh_host'] }}

[galera]
{% for node in groups['galera'] %}
{{ node }} ansible_ssh_host={{ hostvars[node]['ansible_ssh_host'] }}
{% endfor %}

[puppet]
puppetnode ansible_ssh_host={{ hostvars['puppetnode']['ansible_ssh_host'] }}
