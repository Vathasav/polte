[api]
{% for node in groups['api'] %}
{{ node }} ansible_ssh_host={{ hostvars[node]['ansible_ssh_host'] }}
{% endfor %}

[galera]
{% for node in groups['galera'] %}
{{ node }} ansible_ssh_host={{ hostvars[node]['ansible_ssh_host'] }}
{% endfor %}

[puppet]
puppet-node ansible_ssh_host={{ hostvars['puppet-node']['ansible_ssh_host'] }}

[ldap]
ldap-node ansible_ssh_host={{ hostvars['ldap-node']['ansible_ssh_host'] }}
